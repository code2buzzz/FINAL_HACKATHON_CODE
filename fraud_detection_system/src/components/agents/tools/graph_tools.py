from langchain_neo4j import Neo4jGraph

# graph = Neo4jGraph()


def investigate_network(tx):

    # query = """
    # MATCH (c:Customer)-[:USES]->(d:Device)
    # WHERE d.device_id = $device

    # RETURN count(c) as shared_users
    # """

    # result = graph.query(query, params={"device": tx["device_id"]})

    # return result

    pass
