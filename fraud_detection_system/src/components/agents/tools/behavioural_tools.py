from langchain_core.tools import tool

# @tool
# def merchant_behavior_profile(merchant_id: str) -> str:
#     """
#     Fetch behavioral profile of a merchant (spend patterns, risk signals).
#     """
#     return behavioral_retriever.search(f"merchant:{merchant_id}")


# @tool
# def device_behavior_profile(device_id: str) -> str:
#     """
#     Fetch behavioral fingerprint of a device.
#     """
#     return behavioral_retriever.search(f"device:{device_id}")
